name: PSGitHubActions Workflow
on: [workflow_dispatch]
jobs:      
  RGQuery:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v3
    - name: query resource group
      run: pwsh .\AzPS\RGquery.ps1 ${{ fromJson(vars.AZCONNECTIONJSON).clientId }} 
